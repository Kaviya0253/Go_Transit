# Settings for hosting the backend (e.g. on Render). Locally none of these are needed.

# Whole contents of serviceAccountKey.json, as one line of JSON
FIREBASE_SERVICE_ACCOUNT=

# The website's address; only this site may call the API. Several allowed, comma separated.
FRONTEND_URL=https://your-site.vercel.app
